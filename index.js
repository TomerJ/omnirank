const chalk = require("chalk")
const express = require("express")
const app = express()
const chalk = require("chalk")
console.log(chalk.green("-----------------"))
console.log(chalk.green("LOADING OMNIRANK."))
console.log(chalk.green("-----------------"))

app.listen(8172)
